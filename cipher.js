


//I need to learn how to use [...] operator!
// function encode(str) {
//   return [...str].map(c=>String.fromCharCode(c.charCodeAt()*6)).join('');
// }
//
// function decode(str) {
//   return [...str].map(c=>String.fromCharCode(c.charCodeAt()/6)).join('');
// }
